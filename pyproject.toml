[project]
name = "kubernetes-cluster-infra-rca-platform"
version = "0.1.0"
description = "Node evidence collector for the Kubernetes Cluster Infra RCA Platform"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8,<9",
    "PyYAML>=6,<7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
